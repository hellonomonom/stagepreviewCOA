# Nginx configuration for Stage Preview COA
# Place this file at: /etc/nginx/sites-available/stagepreview-coa
# Then create symlink: sudo ln -s /etc/nginx/sites-available/stagepreview-coa /etc/nginx/sites-enabled/
# Test config: sudo nginx -t
# Reload: sudo systemctl reload nginx
#
# SUBDOMAIN SETUP:
# Configured for: preview.anyma.com
# Server IP: 157.230.118.34
# See SUBDOMAIN_SETUP.md for complete instructions

# Upstream for Node.js backend
upstream nodejs_backend {
    server 127.0.0.1:8080;
    keepalive 64;
}

server {
    listen 80;
    # Subdomain: preview.anyma.com
    server_name preview.anyma.com;
    
    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # For initial setup without SSL, use this block:
    client_max_body_size 100M;
    
    # Logging
    access_log /var/log/nginx/stagepreview-coa-access.log;
    error_log /var/log/nginx/stagepreview-coa-error.log;

    # Serve static files directly (optional optimization)
    location /assets/ {
        alias /var/www/stagepreview-coa/dist/assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # WebSocket support for NDI streaming
    location /ndi/ws {
        proxy_pass http://nodejs_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    # Backend API routes
    location /ndi/ {
        proxy_pass http://nodejs_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_buffering off;
    }

    # Health check
    location /health {
        proxy_pass http://nodejs_backend;
        access_log off;
    }

    # Frontend SPA - serve from Node.js (which serves dist/)
    location / {
        proxy_pass http://nodejs_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS configuration (uncomment and configure after SSL certificate setup)
# IMPORTANT: HTTPS is REQUIRED for WebXR to work!
# After running: sudo certbot --nginx -d preview.anyma.com
# Certbot will usually auto-configure this, but here's the manual config:
# server {
#     listen 443 ssl http2;
#     # Subdomain: preview.anyma.com
#     server_name preview.anyma.com;
# 
#     # SSL certificate paths (auto-configured by certbot)
#     ssl_certificate /etc/letsencrypt/live/preview.anyma.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/preview.anyma.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
# 
#     client_max_body_size 100M;
# 
#     access_log /var/log/nginx/stagepreview-coa-ssl-access.log;
#     error_log /var/log/nginx/stagepreview-coa-ssl-error.log;
# 
#     location /assets/ {
#         alias /var/www/stagepreview-coa/dist/assets/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# 
#     location /ndi/ws {
#         proxy_pass http://nodejs_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 86400;
#     }
# 
#     location /ndi/ {
#         proxy_pass http://nodejs_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#         proxy_buffering off;
#     }
# 
#     location /health {
#         proxy_pass http://nodejs_backend;
#         access_log off;
#     }
# 
#     location / {
#         proxy_pass http://nodejs_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

